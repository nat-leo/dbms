[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dbms"
version = "0.0.1"
dependencies = [
  "logging",
]

[tool.setuptools.packages]
find = {}

[tool.pytest.ini_options]
pythonpath = [
  "dbms",
]

[project.scripts]
run = "dbms:hello"

